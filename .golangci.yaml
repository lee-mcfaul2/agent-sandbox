version: "2"
linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - revive
formatters:
  enable:
    - gofmt
issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
