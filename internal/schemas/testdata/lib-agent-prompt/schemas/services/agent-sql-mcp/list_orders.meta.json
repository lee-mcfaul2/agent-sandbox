{
  "description": "List orders placed by a specific customer. REQUIRES the customer's numeric customer_id; if you only have a name, email, or phone, call search_customer FIRST to convert that into a customer_id, then call this tool. Optional 'since' (ISO 8601 datetime) and 'limit' (max 100) narrow the result set.",
  "requires_permissions": [],
  "write": false
}
